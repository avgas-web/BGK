# Проверка и восстановление газлайтинг-эффектов

## ✅ Все задачи выполнены

### 1. Кнопка "Показать телефон" в футере

**Расположение:** Футер, секция "Контакты"
**Функциональность:**
- По умолчанию: "Показать телефон"
- При нажатии: "avgas85@mail.ru (вам показалось)"
- Повторное нажатие возвращает к "Показать телефон"

**Код:**
```tsx
<button
  onClick={() => setShowPhoneGaslight(!showPhoneGaslight)}
  className="hover:text-lime transition-colors"
>
  {showPhoneGaslight ? 'avgas85@mail.ru (вам показалось)' : 'Показать телефон'}
</button>
```

**Психологический эффект:** Пользователь нажимает "Показать телефон", но видит email с припиской "вам показалось". Демонстрация того, как интерфейс обманывает ожидания.

---

### 2. Фейковые подсказки в тесте

**Компонент:** `TestPage.tsx`
**Частота:** 50% вероятность при каждом ответе
**Задержка:** 2 секунды после ответа
**Длительность:** 3 секунды

**Сообщения:**
- "Вы уже отвечали на этот вопрос. Или вам показалось?"

**Визуальное оформление:**
- Фиолетовая рамка слева
- Иконка "СИСТЕМА ПАМЯТИ"
- Появляется в нижней части экрана

**Код:**
```tsx
if (gaslightingEnabled && !clarityMode && Math.random() > 0.5) {
  setTimeout(() => {
    setShowMemoryMessage('Вы уже отвечали на этот вопрос. Или вам показалось?');
    setTimeout(() => setShowMemoryMessage(null), 3000);
  }, 2000);
}
```

---

### 3. Динамическая смена вопроса в тесте

**Компонент:** `TestPage.tsx`
**Частота:** 70% вероятность при каждом ответе (Math.random() > 0.3)
**Задержка:** 3 секунды после ответа
**Условие:** Вопрос должен иметь альтернативы

**Механика:**
1. Пользователь отвечает на вопрос
2. Через 3 секунды вопрос меняется на альтернативный
3. Если ответ уже был дан, он сбрасывается
4. Появляется метка "[ПЕРЕПИСАН]"

**Пример:**
- Исходный вопрос: "Какого цвета был заголовок на предыдущем экране?"
- Альтернатива: "Какой шрифт использовался в заголовке?"

**Код:**
```tsx
if (gaslightingEnabled && !clarityMode && Math.random() > 0.3) {
  const currentQ = selectedQuestions[currentQuestion];
  if (currentQ.alternatives && currentQ.alternatives.length > 0) {
    setTimeout(() => {
      const alt = currentQ.alternatives![Math.floor(Math.random() * currentQ.alternatives!.length)];
      setSelectedQuestions(prev => prev.map((q, i) => 
        i === currentQuestion ? { ...q, question: alt.question, options: alt.options, correct: alt.correct } : q
      ));
      setQuestionRewritten(true);
      if (answers[currentQuestion] !== null) {
        setAnswers(prev => prev.map((a, i) => i === currentQuestion ? null : a));
        setAnswerChanged(true);
      }
    }, 3000);
  }
}
```

---

### 4. Изменение ответов в тесте

**Компонент:** `TestPage.tsx`
**Частота:** Связано с динамической сменой вопроса
**Условие:** Если вопрос изменился и ответ уже был дан

**Механика:**
1. Пользователь отвечает на вопрос
2. Через 3 секунды вопрос меняется
3. Ответ сбрасывается на null
4. Появляется метка "* ваш ответ был сброшен"

**Визуальное оформление:**
- Оранжевый текст
- Мелкий шрифт
- Появляется под вопросом

**Код:**
```tsx
if (answers[currentQuestion] !== null) {
  setAnswers(prev => prev.map((a, i) => i === currentQuestion ? null : a));
  setAnswerChanged(true);
}
```

---

### 5. Постоянно всплывающие окошки с напоминаниями

**Компонент:** `GaslightEffects.tsx`

#### A. Системные уведомления
**Частота:** Каждые 20 секунд с вероятностью 15%
**Длительность:** 4 секунды
**Расположение:** Правый верхний угол

**Сообщения:**
- "Калибровка завершена. Результаты сохранены."
- "Вы уже проходили тест 12.04.2025"
- "Обнаружена аномалия восприятия"
- "Синхронизация памяти..."
- "Ваш прогресс: 98%"
- "Попытка №3 за сегодня"
- "Сессия восстановлена"

**Код:**
```tsx
intervalsRef.current.push(setInterval(() => {
  if (Math.random() > 0.85) {
    const notifications = GASLIGHT_CONFIG.phrases.notifications;
    const n = notifications[Math.floor(Math.random() * notifications.length)];
    setEffects(prev => ({ ...prev, notification: { id: Date.now(), ...n } }));
    setTimeout(() => setEffects(prev => ({ ...prev, notification: null })), 4000);
  }
}, timings.notifications.interval * capabilities.slowdownFactor));
```

#### B. Шёпот
**Частота:** Каждые 20 секунд с вероятностью 35%
**Длительность:** 3 секунды
**Расположение:** Центр экрана

**Сообщения:**
- "Ты сам это выбрал"
- "Так было всегда"
- "Ты уверен?"
- "Это не первый раз"
- "Ты уже видел это"
- "Ничего не изменилось"
- "Ты помнишь?"
- "Это тебе снилось"

**Код:**
```tsx
intervalsRef.current.push(setInterval(() => {
  if (Math.random() > 0.65) {
    const whispers = GASLIGHT_CONFIG.phrases.whispers;
    const whisper = whispers[Math.floor(Math.random() * whispers.length)];
    setEffects(prev => ({ ...prev, whisper }));
    setTimeout(() => setEffects(prev => ({ ...prev, whisper: '' })), 3000);
  }
}, timings.notifications.interval * capabilities.slowdownFactor));
```

#### C. Манипуляция памятью
**Частота:** Каждые 25 секунд с вероятностью 12%
**Длительность:** 3 секунды
**Расположение:** Нижняя часть экрана

**Сообщения:**
- "Вы уже нажали эту кнопку"
- "Вы читали этот абзац"
- "Вы были здесь 5 минут назад"
- "Вы уже отвечали на этот вопрос"
- "Вы уже видели этот экран"

**Код:**
```tsx
intervalsRef.current.push(setInterval(() => {
  if (Math.random() > 0.88) {
    const memories = GASLIGHT_CONFIG.phrases.memoryGaslight;
    const memory = memories[Math.floor(Math.random() * memories.length)];
    setEffects(prev => ({ ...prev, memoryGaslight: memory }));
    setTimeout(() => setEffects(prev => ({ ...prev, memoryGaslight: null })), 3000);
  }
}, timings.memoryGaslight.interval * capabilities.slowdownFactor));
```

---

### 6. Режим спектакля - все эффекты активны

**Компонент:** `App.tsx`
**Логика:**
```tsx
const effectsActive = gaslightingEnabled && !clarityMode && !stopWordActive;
```

**Условия активации эффектов:**
- `gaslightingEnabled = true` - газлайтинг включён
- `clarityMode = false` - режим ясности отключён
- `stopWordActive = false` - стоп-слово не активировано

**В режиме спектакля работают:**
- ✅ Все визуальные эффекты (jitter, color shift, VHS, и т.д.)
- ✅ Все всплывающие окна (уведомления, шёпот, память)
- ✅ Тревожные окна с ловушками
- ✅ Страшные эффекты (BSOD, трещины, экран блокировки)
- ✅ Обвинение в неоплаченном интернете
- ✅ Сообщения о списании денег
- ✅ Насмешливые комментарии
- ✅ Таймер ложной срочности
- ✅ Динамический FAQ с пренебрежительными ответами
- ✅ Кнопка "показать телефон" с газлайтингом

**В режиме ясности отключены:**
- ❌ Все газлайтинг-эффекты
- ❌ Все всплывающие окна
- ❌ Все страшные эффекты
- ❌ Кнопка "показать телефон" показывает email без приписки

---

## 📊 Таблица периодичности эффектов

| Эффект | Частота | Вероятность | Длительность | Режим |
|--------|---------|-------------|--------------|-------|
| **Системные уведомления** | 20 сек | 15% | 4 сек | Спектакль |
| **Шёпот** | 20 сек | 35% | 3 сек | Спектакль |
| **Манипуляция памятью** | 25 сек | 12% | 3 сек | Спектакль |
| **Тревожные окна** | 25 сек | 10% | 4 сек | Спектакль |
| **Ложные подтверждения** | - | 50% от окон | 3 сек | Спектакль |
| **BSOD** | 60 сек | 5% | 3 сек | Спектакль |
| **Экран блокировки** | 60 сек | 5% | 2 сек | Спектакль |
| **Трещины на экране** | 60 сек | 5% | 4 сек | Спектакль |
| **Красное предупреждение** | 30 сек | 10% | 1.5 сек | Спектакль |
| **Обвинение в интернете** | 30 сек | 10% | 3.5 сек | Спектакль |
| **Насмешливые комментарии** | 20 сек | 20% | 3.5 сек | Спектакль |
| **Таймер срочности** | 15 сек | 30% | 5 сек | Спектакль |
| **Смена вопроса в тесте** | При ответе | 70% | 3 сек | Спектакль |
| **Фейковые подсказки** | При ответе | 50% | 3 сек | Спектакль |
| **Динамический FAQ** | При клике | 50% | - | Спектакль |

---

## 🎯 Итоговый результат

### Восстановлено:
- ✅ Кнопка "Показать телефон" в футере с газлайтингом
- ✅ Фейковые подсказки в тесте (50% вероятность)
- ✅ Динамическая смена вопроса в тесте (70% вероятность)
- ✅ Изменение ответов в тесте
- ✅ Постоянно всплывающие окошки с напоминаниями
- ✅ Все эффекты активны в режиме спектакля

### Проверено:
- ✅ Все эффекты работают корректно
- ✅ Периодичность появления соответствует настройкам
- ✅ В режиме спектакля ничто не мешает газлайтингу
- ✅ В режиме ясности все эффекты отключены

---

## 🚀 Готово к использованию

Проект полностью восстановлен и готов к деплою:

- ✅ Кнопка "Показать телефон" работает
- ✅ Фейковые подсказки появляются
- ✅ Вопросы меняются динамически
- ✅ Ответы сбрасываются
- ✅ Всплывающие окна работают постоянно
- ✅ Режим спектакля активен

**Контакты:** avgas85@mail.ru

**Бесконечность — не предел. Особенно бесконечность осознанности.** ✨
